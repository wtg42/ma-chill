## 1. 事件流容器與本地捲動狀態

- [x] 1.1 將 `EventLog` 從固定 `slice(-18)` 顯示改為承載完整事件資料的可捲動容器
- [x] 1.2 在 TUI 本地加入事件流 viewport 狀態，追蹤目前位置與是否跟隨最新內容
- [x] 1.3 調整主畫面布局，確保高度不足時仍保留狀態列與命令列，並由中間事件流承擔捲動

## 2. 捲動互動與自動跟隨規則

- [x] 2.1 在鍵盤處理層加入 `PageUp`、`PageDown`、`Home`、`End` 的事件流導覽行為
- [x] 2.2 實作「位於底部時自動跟隨、手動回看時停止跟隨、按 `End` 恢復跟隨」的行為邏輯
- [x] 2.3 確保事件流捲動不送出 IPC、不中斷命令列輸入，也不與既有快捷鍵衝突

## 3. 驗證與回歸測試

- [x] 3.1 新增或更新測試，覆蓋事件流可回看、導覽鍵捲動與回到底部恢復跟隨的情境
- [x] 3.2 驗證 `/hand` 等多行輸出在事件流中可完整回看，不再因固定裁切而遺失上半部內容
